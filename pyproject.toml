[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "deep-agent-support-assistant"
version = "0.1.0"
description = "State-driven support troubleshooting assistant"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
