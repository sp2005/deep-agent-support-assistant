"""Reserved module for domain schemas.

Workflow payloads currently live in ``graph.state`` so LangGraph has one shared
state contract. New standalone API schemas can be added here without moving
that graph contract.
"""
