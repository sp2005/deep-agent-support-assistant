"""Application settings namespace.

Runtime configuration currently comes from environment variables consumed by
the shared LLM factory. This module remains available for future typed settings
that are not provider-specific.
"""
